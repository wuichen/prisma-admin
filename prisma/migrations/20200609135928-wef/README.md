# Migration `20200609135928-wef`

This migration has been generated by I Chen Wu at 6/9/2020, 1:59:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Address" (
"companyId" integer   ,"id" SERIAL,"info" text  NOT NULL ,"json" text   ,"name" text  NOT NULL ,"type" text  NOT NULL ,"userId" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Staff" (
"companyId" integer  NOT NULL ,"contactId" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text   ,"firstName" text   ,"id" SERIAL,"lastName" text   ,"role" text  NOT NULL ,"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Card" (
"cardType" text  NOT NULL ,"id" SERIAL,"lastFourDigit" integer  NOT NULL ,"name" text  NOT NULL ,"type" text  NOT NULL ,"userId" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Contact" (
"companyId" integer   ,"id" SERIAL,"number" text  NOT NULL ,"type" text   ,"userId" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Company" (
"addressId" integer   ,"contactId" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"deliveryDetailId" integer  NOT NULL ,"description" text  NOT NULL ,"id" SERIAL,"name" text  NOT NULL ,"ownerId" text   ,"platformId" integer  NOT NULL ,"platformSlug" text  NOT NULL ,"previewUrl" text   ,"promotion" text   ,"slug" text  NOT NULL ,"thumbnailUrl" text   ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."DeliveryDetail" (
"charge" text  NOT NULL ,"companyId" integer   ,"id" SERIAL,"isFree" boolean  NOT NULL ,"minimumOrder" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Coupon" (
"code" text  NOT NULL ,"discountInPercent" integer  NOT NULL DEFAULT 0,"id" SERIAL,"image" text  NOT NULL ,"name" text  NOT NULL ,"numberOfCoupon" integer  NOT NULL DEFAULT 0,"numberOfUsedCoupon" integer  NOT NULL DEFAULT 0,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Country" (
"code" integer  NOT NULL ,"currency" text  NOT NULL ,"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Platform" (
"bannerImg" text   ,"countryId" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"description" text   ,"id" SERIAL,"name" text   ,"ownerId" text  NOT NULL ,"platformType" text   ,"slug" text  NOT NULL ,"taxRate" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."User" (
"coverImage" text   ,"coverImagePublicId" text   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"firstName" text   ,"googleId" text   ,"id" text  NOT NULL ,"image" text   ,"imagePublicId" text   ,"income" Decimal(65,30)   ,"isOnline" boolean  NOT NULL DEFAULT false,"lastName" text   ,"token" text   ,"username" text   ,"walletId" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Customer" (
"address" text   ,"avatar" text   ,"birthday" timestamp(3)   ,"city" text   ,"companyId" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"firstName" text  NOT NULL ,"firstSeen" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,"groups" text []  ,"hasNewsLetter" boolean   DEFAULT false,"hasOrdered" boolean   DEFAULT false,"id" SERIAL,"image" text   ,"lastName" text  NOT NULL ,"lastSeen" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,"latestPurchase" timestamp(3)   ,"nbOrders" integer   ,"totalOrder" integer  NOT NULL DEFAULT 0,"totalSpent" Decimal(65,30)   DEFAULT 0,"userId" text  NOT NULL ,"zipcode" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Category" (
"categoryId" integer   ,"icon" text   ,"id" SERIAL,"name" text  NOT NULL ,"platformId" integer  NOT NULL ,"slug" text  NOT NULL ,"type" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Product" (
"companyId" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"description" text   ,"discountInPercent" integer   ,"gallery" text []  ,"height" Decimal(65,30)   ,"id" SERIAL,"image" text   ,"name" text   ,"price" Decimal(65,30)   ,"salePrice" Decimal(65,30)   ,"sku" text   ,"slug" text  NOT NULL ,"stock" integer   ,"thumbnail" text   ,"type" text  NOT NULL ,"unit" text   ,"width" Decimal(65,30)   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."OrderItem" (
"id" SERIAL,"image" text   ,"name" text   ,"orderId" integer  NOT NULL ,"price" Decimal(65,30)  NOT NULL ,"productId" integer  NOT NULL ,"quantity" integer  NOT NULL ,"weight" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Order" (
"companyId" integer   ,"contactId" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"customerId" integer   ,"deliveryAddressId" integer  NOT NULL ,"deliveryFees" Decimal(65,30)  NOT NULL ,"deliveryTime" timestamp(3)   ,"id" SERIAL,"paymentMethod" text  NOT NULL ,"reference" text   ,"returned" boolean  NOT NULL DEFAULT false,"status" text   ,"subtotal" Decimal(65,30)  NOT NULL ,"taxRate" Decimal(65,30)  NOT NULL ,"taxes" Decimal(65,30)  NOT NULL ,"total" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Invoice" (
"companyId" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"customerId" integer  NOT NULL ,"deliveryFees" Decimal(65,30)   ,"id" SERIAL,"orderId" integer  NOT NULL ,"subtotal" Decimal(65,30)   ,"taxRate" Decimal(65,30)   ,"taxes" Decimal(65,30)   ,"total" Decimal(65,30)   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Review" (
"comment" text   ,"companyId" integer   ,"createdAt" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,"customerId" integer   ,"id" SERIAL,"orderId" integer   ,"productId" integer   ,"rating" integer   ,"status" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."_CategoryToCompany" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_CategoryToProduct" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "Company.slug" ON "public"."Company"("slug")

CREATE UNIQUE INDEX "Company_addressId" ON "public"."Company"("addressId")

CREATE UNIQUE INDEX "Company_contactId" ON "public"."Company"("contactId")

CREATE UNIQUE INDEX "Company_deliveryDetailId" ON "public"."Company"("deliveryDetailId")

CREATE UNIQUE INDEX "Country.code" ON "public"."Country"("code")

CREATE UNIQUE INDEX "Platform.slug" ON "public"."Platform"("slug")

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.username" ON "public"."User"("username")

CREATE UNIQUE INDEX "Category.slug" ON "public"."Category"("slug")

CREATE UNIQUE INDEX "Product.slug" ON "public"."Product"("slug")

CREATE UNIQUE INDEX "_CategoryToCompany_AB_unique" ON "public"."_CategoryToCompany"("A","B")

CREATE  INDEX "_CategoryToCompany_B_index" ON "public"."_CategoryToCompany"("B")

CREATE UNIQUE INDEX "_CategoryToProduct_AB_unique" ON "public"."_CategoryToProduct"("A","B")

CREATE  INDEX "_CategoryToProduct_B_index" ON "public"."_CategoryToProduct"("B")

ALTER TABLE "public"."Address" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Staff" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Staff" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Staff" ADD FOREIGN KEY ("contactId")REFERENCES "public"."Contact"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Card" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Contact" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Company" ADD FOREIGN KEY ("addressId")REFERENCES "public"."Address"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Company" ADD FOREIGN KEY ("contactId")REFERENCES "public"."Contact"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Company" ADD FOREIGN KEY ("platformId")REFERENCES "public"."Platform"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Company" ADD FOREIGN KEY ("ownerId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Company" ADD FOREIGN KEY ("deliveryDetailId")REFERENCES "public"."DeliveryDetail"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Platform" ADD FOREIGN KEY ("ownerId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Platform" ADD FOREIGN KEY ("countryId")REFERENCES "public"."Country"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Customer" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Customer" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Category" ADD FOREIGN KEY ("platformId")REFERENCES "public"."Platform"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Category" ADD FOREIGN KEY ("categoryId")REFERENCES "public"."Category"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Product" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."OrderItem" ADD FOREIGN KEY ("orderId")REFERENCES "public"."Order"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."OrderItem" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Order" ADD FOREIGN KEY ("customerId")REFERENCES "public"."Customer"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Order" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Order" ADD FOREIGN KEY ("deliveryAddressId")REFERENCES "public"."Address"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Order" ADD FOREIGN KEY ("contactId")REFERENCES "public"."Contact"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Invoice" ADD FOREIGN KEY ("orderId")REFERENCES "public"."Order"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Invoice" ADD FOREIGN KEY ("customerId")REFERENCES "public"."Customer"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Invoice" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Review" ADD FOREIGN KEY ("orderId")REFERENCES "public"."Order"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Review" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Review" ADD FOREIGN KEY ("customerId")REFERENCES "public"."Customer"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Review" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToCompany" ADD FOREIGN KEY ("A")REFERENCES "public"."Category"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToCompany" ADD FOREIGN KEY ("B")REFERENCES "public"."Company"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToProduct" ADD FOREIGN KEY ("A")REFERENCES "public"."Category"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToProduct" ADD FOREIGN KEY ("B")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200609135928-wef
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,326 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Address {
+  id        Int      @default(autoincrement()) @id
+  type      String
+  name      String
+  json      String?
+  info      String
+  companyId Int?
+  company   Company?
+  userId    String?
+  user      User?    @relation(fields: [userId], references: [id])
+  // @onDelete(SET_NULL)
+  orders    Order[]
+}
+
+model Staff {
+  id        Int      @default(autoincrement()) @id
+  userId    String
+  user      User     @relation(fields: [userId], references: [id])
+  company   Company  @relation(fields: [companyId], references: [id])
+  companyId Int
+  role      String
+  createdAt DateTime @default(now())
+  firstName String?
+  lastName  String?
+  contact   Contact? @relation(fields: [contactId], references: [id])
+  contactId Int?
+  email     String?
+}
+
+model Card {
+  id            Int     @default(autoincrement()) @id
+  type          String
+  cardType      String
+  name          String
+  lastFourDigit Int
+  userId        String?
+  user          User?   @relation(fields: [userId], references: [id])
+}
+
+model Contact {
+  id        Int      @default(autoincrement()) @id
+  type      String?
+  number    String
+  companyId Int?
+  company   Company?
+  userId    String?
+  user      User?    @relation(fields: [userId], references: [id])
+  // @onDelete(SET_NULL)
+  orders    Order[]
+  // @onDelete(SET_NULL)
+  staffs    Staff[]
+}
+
+model Company {
+  id               Int            @default(autoincrement()) @id
+  slug             String         @unique
+  name             String
+  description      String
+  previewUrl       String?
+  thumbnailUrl     String?
+  platformSlug     String
+  address          Address?       @relation(fields: [addressId], references: [id])
+  addressId        Int?
+  contact          Contact?       @relation(fields: [contactId], references: [id])
+  contactId        Int?
+  platformId       Int
+  platform         Platform       @relation(fields: [platformId], references: [id])
+  // @onDelete(CASCADE)
+  products         Product[]
+  // @onDelete(CASCADE)
+  customers        Customer[]
+  // @onDelete(SET_NULL)
+  orders           Order[]
+  owner            User?          @relation(fields: [ownerId], references: [id])
+  ownerId          String?
+  // @onDelete(SET_NULL)
+  invoices         Invoice[]
+  reviews          Review[]
+  staffs           Staff[]
+  deliveryDetail   DeliveryDetail @relation(fields: [deliveryDetailId], references: [id])
+  deliveryDetailId Int
+  promotion        String?
+  createdAt        DateTime       @default(now())
+  updatedAt        DateTime       @updatedAt
+  categories       Category[]     @relation(references: [id])
+}
+
+model DeliveryDetail {
+  id           Int      @default(autoincrement()) @id
+  isFree       Boolean
+  charge       String
+  minimumOrder Float
+  companyId    Int?
+  company      Company?
+}
+
+model Coupon {
+  id                 Int    @default(autoincrement()) @id
+  name               String
+  code               String
+  image              String
+  discountInPercent  Int    @default(0)
+  numberOfCoupon     Int    @default(0)
+  numberOfUsedCoupon Int    @default(0)
+}
+
+model Country {
+  id        Int        @default(autoincrement()) @id
+  code      Int        @unique
+  name      String
+  currency  String
+  // @onDelete(SET_NULL)
+  platforms Platform[]
+
+}
+
+model Platform {
+  id           Int        @default(autoincrement()) @id
+  name         String?
+  bannerImg    String?
+  slug         String     @unique
+  ownerId      String
+  owner        User       @relation(fields: [ownerId], references: [id])
+  description  String?
+  // @onDelete(SET_NULL)
+  companies    Company[]
+  // @onDelete(SET_NULL)
+  categories   Category[]
+  platformType String?
+  countryId    Int?
+  country      Country?   @relation(fields: [countryId], references: [id])
+  taxRate      Float
+  createdAt    DateTime   @default(now())
+}
+
+model User {
+  id                 String     @id
+  createdAt          DateTime   @default(now())
+  email              String     @unique
+  firstName          String?
+  lastName           String?
+  token              String?
+  googleId           String?
+  walletId           String?
+  income             Float?
+  username           String?    @unique
+  image              String?
+  imagePublicId      String?
+  coverImage         String?
+  coverImagePublicId String?
+  isOnline           Boolean    @default(false)
+  // @onDelete(SET_NULL)
+  companies          Company[]
+  // @onDelete(SET_NULL)
+  platforms          Platform[]
+  // @onDelete(SET_NULL)
+  addresses          Address[]
+  // @onDelete(CASCADE)
+  cards              Card[]
+  // @onDelete(SET_NULL)
+  contacts           Contact[]
+  // @onDelete(SET_NULL)
+  customers          Customer[]
+  staffs             Staff[]
+}
+
+model Customer {
+  id             Int       @default(autoincrement()) @id
+  firstName      String
+  lastName       String
+  email          String
+  address        String?
+  zipcode        String?
+  city           String?
+  avatar         String?
+  birthday       DateTime?
+  firstSeen      DateTime? @default(now())
+  lastSeen       DateTime? @default(now())
+  hasOrdered     Boolean?  @default(false)
+  latestPurchase DateTime?
+  hasNewsLetter  Boolean?  @default(false)
+  groups         String[]
+  nbOrders       Int?
+  // @onDelete(CASACADE)
+  reviews        Review[]
+  // @onDelete(SET_NULL)
+  invoices       Invoice[]
+  orders         Order[]
+  company        Company?  @relation(fields: [companyId], references: [id])
+  companyId      Int?
+  userId         String
+  user           User      @relation(fields: [userId], references: [id])
+  image          String?
+  totalOrder     Int       @default(0)
+  totalSpent     Float?    @default(0)
+  createdAt      DateTime  @default(now())
+}
+
+model Category {
+  id         Int        @default(autoincrement()) @id
+  name       String
+  // @onDelete(SET_NULL)
+  products   Product[]
+  platform   Platform   @relation(fields: [platformId], references: [id])
+  platformId Int
+  slug       String     @unique
+  type       String
+  icon       String?
+  categoryId Int?
+  parent     Category?  @relation("Children", fields: [categoryId], references: [id])
+  // @onDelete(SET_NULL)
+  children   Category[] @relation("Children")
+  companies  Company[]
+}
+
+model Product {
+  id                Int         @default(autoincrement()) @id
+  width             Float?
+  height            Float?
+  price             Float?
+  thumbnail         String?
+  image             String?
+  description       String?
+  stock             Int?
+  // @onDelete(CASCADE)
+  review            Review[]
+  // @onDelete(SET_NULL)
+  orderItems        OrderItem[]
+  company           Company?    @relation(fields: [companyId], references: [id])
+  companyId         Int?
+  sku               String?
+  name              String?
+  slug              String      @unique
+  salePrice         Float?
+  discountInPercent Int?
+  type              String
+  gallery           String[]
+  // @onDelete(SET_NULL)
+  categories        Category[]
+  unit              String?
+  createdAt         DateTime    @default(now())
+
+}
+
+model OrderItem {
+  id        Int     @default(autoincrement()) @id
+  order     Order   @relation(fields: [orderId], references: [id])
+  orderId   Int
+  quantity  Int
+  name      String?
+  weight    String?
+  price     Float
+  productId Int
+  product   Product @relation(fields: [productId], references: [id])
+  image     String?
+}
+
+model Order {
+  id                Int         @default(autoincrement()) @id
+  reference         String?
+  customerId        Int?
+  customer          Customer?   @relation(fields: [customerId], references: [id])
+  subtotal          Float
+  deliveryFees      Float
+  taxRate           Float
+  taxes             Float
+  total             Float
+  status            String?
+  returned          Boolean     @default(false)
+  // @onDelete(SET_NULL)
+  invoice           Invoice[]
+  // @onDelete(SET_NULL)
+  reviews           Review[]
+  // @onDelete(SET_NULL)
+  orderItems        OrderItem[]
+  company           Company?    @relation(fields: [companyId], references: [id])
+  companyId         Int?
+  deliveryTime      DateTime?
+  createdAt         DateTime    @default(now())
+  paymentMethod     String
+  deliveryAdress    Address     @relation(fields: [deliveryAddressId], references: [id])
+  deliveryAddressId Int
+  contact           Contact?    @relation(fields: [contactId], references: [id])
+  contactId         Int?
+}
+
+model Invoice {
+  id           Int      @default(autoincrement()) @id
+  createdAt    DateTime @default(now())
+  orderId      Int
+  order        Order    @relation(fields: [orderId], references: [id])
+  customerId   Int
+  customer     Customer @relation(fields: [customerId], references: [id])
+  subtotal     Float?
+  deliveryFees Float?
+  taxRate      Float?
+  taxes        Float?
+  total        Float?
+  company      Company? @relation(fields: [companyId], references: [id])
+  companyId    Int?
+}
+
+model Review {
+  id         Int       @default(autoincrement()) @id
+  createdAt  DateTime? @default(now())
+  status     String?
+  orderId    Int?
+  order      Order?    @relation(fields: [orderId], references: [id])
+  productId  Int?
+  product    Product?  @relation(fields: [productId], references: [id])
+  customerId Int?
+  customer   Customer? @relation(fields: [customerId], references: [id])
+  rating     Int?
+  comment    String?
+  company    Company?  @relation(fields: [companyId], references: [id])
+  companyId  Int?
+}
```


